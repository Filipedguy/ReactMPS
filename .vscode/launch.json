{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "localhost (Chrome)",
      "preLaunchTask": "npm: debug",
      "url": "http://localhost:3000/ReactMPS",
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}